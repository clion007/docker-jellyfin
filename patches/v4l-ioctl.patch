--- a/libavdevice/v4l2.c
+++ b/libavdevice/v4l2.c
@@ -67,13 +67,13 @@ struct video_data {
     int use_libv4l2;
     int (*open_f)(const char *file, int oflag, ...);
     int (*close_f)(int fd);
-    int (*dup_f)(int fd);
-#if HAVE_POSIX_IOCTL
-    int (*ioctl_f)(int fd, int request, ...);
-#else
+    int (*dup_f)(int fd);
+    /* ioctl_f should use unsigned long int to match libv4l2 signature
+     * and be compatible with system ioctl (int gets promoted) */
     int (*ioctl_f)(int fd, unsigned long int request, ...);
-#endif
     ssize_t (*read_f)(int fd, void *buffer, size_t n);
     void *(*mmap_f)(void *start, size_t length, int prot, int flags, int fd, int64_t offset);
     int (*munmap_f)(void *_start, size_t length);
@@ -132,7 +132,7 @@ static int device_open(AVFormatContext *ctx, const char* device_path)
 } while (0)
 
     if (s->use_libv4l2) {
-#if CONFIG_LIBV4L2
+#ifdef CONFIG_LIBV4L2
         SET_WRAPPERS(v4l2_);
 #else
         av_log(ctx, AV_LOG_ERROR, "libavdevice is not built with libv4l2 support.\n");
@@ -140,6 +140,10 @@ static int device_open(AVFormatContext *ctx, const char* device_path)
 #endif
     } else {
         SET_WRAPPERS();
+        /* Cast system ioctl to match the expected signature.
+         * This is safe because int can be promoted to unsigned long int. */
+        s->ioctl_f = (int (*)(int, unsigned long int, ...))ioctl;
     }
 
 #define v4l2_open   s->open_f
@@ -824,7 +828,7 @@ static int v4l2_read_header(AVFormatContext *ctx)
     if (!st)
         return AVERROR(ENOMEM);
 
-#if CONFIG_LIBV4L2
+#ifdef CONFIG_LIBV4L2
     /* silence libv4l2 logging. if fopen() fails v4l2_log_file will be NULL
        and errors will get sent to stderr */
     if (s->use_libv4l2)
