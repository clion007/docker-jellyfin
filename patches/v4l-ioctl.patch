--- a/libavdevice/v4l2.c
+++ b/libavdevice/v4l2.c
@@ -45,7 +45,7 @@
 #include "v4l2-common.h"
 #include <dirent.h>
 
-#if CONFIG_LIBV4L2
+#ifdef CONFIG_LIBV4L2
 #include <libv4l2.h>
 #endif
 
@@ -70,13 +70,9 @@
     int use_libv4l2;
     int (*open_f)(const char *file, int oflag, ...);
     int (*close_f)(int fd);
-    int (*dup_f)(int fd);
-#if HAVE_POSIX_IOCTL
-    int (*ioctl_f)(int fd, int request, ...);
-#else
+    int (*dup_f)(int fd);
+    /* Use unsigned long int to match V4L2 API */
     int (*ioctl_f)(int fd, unsigned long int request, ...);
-#endif
-    ssize_t (*read_f)(int fd, void *buffer, size_t n);
     void *(*mmap_f)(void *start, size_t length, int prot, int flags, int fd, int64_t offset);
     int (*munmap_f)(void *_start, size_t length);
 };
@@ -86,6 +82,18 @@
     int index;
 };
 
+/* Wrapper function to convert unsigned long int to int for system ioctl */
+static int sys_ioctl_wrapper(int fd, unsigned long int request, ...)
+{
+    va_list ap;
+    void *arg;
+    va_start(ap, request);
+    arg = va_arg(ap, void *);
+    va_end(ap);
+    
+    return ioctl(fd, (int)request, arg);
+}
+
 static int device_open(AVFormatContext *ctx, const char* device_path)
 {
     struct video_data *s = ctx->priv_data;
@@ -105,7 +113,7 @@
 } while (0)
 
     if (s->use_libv4l2) {
-#if CONFIG_LIBV4L2
+#ifdef CONFIG_LIBV4L2
         SET_WRAPPERS(v4l2_);
 #else
         av_log(ctx, AV_LOG_ERROR, "libavdevice is not built with libv4l2 support.\n");
@@ -113,6 +121,8 @@
 #endif
     } else {
         SET_WRAPPERS();
+        /* Use wrapper function for system ioctl */
+        s->ioctl_f = sys_ioctl_wrapper;
     }
 
 #define v4l2_open   s->open_f
@@ -831,7 +841,7 @@
     if (!st)
         return AVERROR(ENOMEM);
 
-#if CONFIG_LIBV4L2
+#ifdef CONFIG_LIBV4L2
     /* silence libv4l2 logging. if fopen() fails v4l2_log_file will be NULL
        and errors will get sent to stderr */
     if (s->use_libv4l2)
